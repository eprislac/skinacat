(* hereKitty.ml
This is a comment in OCaml. They can span multiple lines by default, and must be terminated by a following *)

(* to install OCaml in Ubuntu: 
sudo apt-get install ocaml *)

(*to run this file, first compile: 
ocamlc -o hereKitty hereKitty.ml *)

(* then run
 ./hereKitty *)

print_string "Here kitty kitty...\n";;
